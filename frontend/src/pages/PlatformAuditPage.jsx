import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { platformAudit } from '../api/platform'
export default function PlatformAuditPage(){const [rows,setRows]=useState(null);const [error,setError]=useState('');useEffect(()=>{platformAudit().then(setRows).catch(()=>setError('Platform audit history could not be loaded.'))},[]);return <main className="student-page"><header className="student-page-header"><div><Link to="/platform" className="dashboard-link">Back to platform</Link><h1>Platform audit log</h1></div></header>{error?<p className="form-error">{error}</p>:!rows?<p className="student-state">Loading audit history…</p>:!rows.length?<p className="student-state">No Platform actions have been recorded.</p>:<section className="profile-section"><div className="student-table-wrap"><table className="student-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Target</th><th>Summary</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{new Date(row.timestamp).toLocaleString()}</td><td>{row.actor}</td><td>{row.action.replaceAll('_',' ')}</td><td>{row.target_type} #{row.target_id}</td><td>{row.summary||'—'}</td></tr>)}</tbody></table></div></section>}</main>}
