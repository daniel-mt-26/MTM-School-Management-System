import { useEffect, useMemo, useState } from 'react'
import { apiClient } from '../api/client'
import { downloadReceiptPdf, getParentStudentFinance } from '../api/finance'

const days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday']
const money = (currency, value) => `${currency} ${new Intl.NumberFormat(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(value || 0)}`
export default function ParentDashboard() { const [students, setStudents] = useState([]); const [selected, setSelected] = useState(''); const [entries, setEntries] = useState([]); const [finance, setFinance] = useState(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { apiClient('/parent/students/').then((items) => { setStudents(items); setSelected(items[0]?.id ? String(items[0].id) : '') }).catch(() => setError('We could not load your linked children.')).finally(() => setLoading(false)) }, []); useEffect(() => { if (selected) { apiClient(`/parent/students/${selected}/timetable/`).then(setEntries).catch(() => setError('We could not load this class timetable.')); getParentStudentFinance(selected).then(setFinance).catch(() => setError('We could not load this child’s finance information.')) } }, [selected]); const slots = useMemo(() => Array.from(new Set(entries.map((entry) => `${entry.start_time} – ${entry.end_time}`))).sort(), [entries]); const at = (slot, day) => entries.find((entry) => `${entry.start_time} – ${entry.end_time}` === slot && entry.day_of_week === day); if (loading) return <main className="school-profile-state">Loading your children…</main>; return <main className="student-page"><header className="student-page-header"><div><h1>Parent Portal</h1><p>Read-only child timetable and finance information.</p></div></header>{error && <p className="form-error">{error}</p>}{students.length ? <><section className="profile-section"><label className="timetable-selector">Child<select value={selected} onChange={(e) => setSelected(e.target.value)}>{students.map((x) => <option key={x.id} value={x.id}>{x.display_name} · {x.class_name}</option>)}</select></label></section>{finance && <><section className="finance-summary"><div><span>Total Charges</span><strong>{money(finance.currency, finance.total_charges)}</strong></div><div><span>Total Payments</span><strong>{money(finance.currency, finance.total_payments)}</strong></div><div><span>Outstanding Balance</span><strong>{money(finance.currency, finance.outstanding_balance)}</strong></div></section><section className="profile-section"><h2>Payments & Receipts</h2>{finance.payments.length ? <div className="history-list">{finance.payments.map((payment) => <div key={payment.id}><strong>{money(payment.currency, payment.amount)} · {payment.fee_name}{payment.is_reversed ? ' · Reversed' : ''}</strong><span>{new Date(payment.paid_at).toLocaleString()} · {payment.method}</span></div>)}</div> : <p className="muted-copy">No payments are available for this child.</p>}{finance.receipts.map((receipt) => <div className="history-list" key={receipt.id}><div><strong>{receipt.receipt_number}{receipt.is_reversed ? ' · Reversed' : ''}</strong><button className="inline-button" onClick={() => downloadReceiptPdf(receipt.id, true)}>Download Receipt</button></div></div>)}</section></>}<section className="profile-section"><h2>Class Timetable</h2><div className="student-table-wrap"><table className="student-table"><thead><tr><th>Time</th>{days.map((day) => <th key={day}>{day}</th>)}</tr></thead><tbody>{slots.length ? slots.map((slot) => <tr key={slot}><th>{slot}</th>{days.map((day) => <td key={day}>{at(slot, day)?.display_label || ''}</td>)}</tr>) : <tr><td colSpan="6" className="empty-cell">No timetable entries are available.</td></tr>}</tbody></table></div></section></> : <div className="student-state">No children are currently linked to this account.</div>}</main> }
