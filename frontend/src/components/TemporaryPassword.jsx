import { useState } from 'react'
export default function TemporaryPassword({ username, password, onDismiss }) { const [copied,setCopied]=useState(false); return <section className="profile-section" role="status"><h2>Temporary password</h2><p>Administrator: <strong>{username}</strong></p><p><code>{password}</code></p><p>Copy this password now. It will not be shown again.</p><button type="button" onClick={()=>{navigator.clipboard?.writeText(password);setCopied(true)}}>{copied?'Copied':'Copy password'}</button><button type="button" className="secondary-button" onClick={onDismiss}>Dismiss</button></section> }
